<?xml version="1.0" encoding="UTF-8"?>
<!--日志级别以及优先级排序: OFF > FATAL > ERROR > WARN > INFO > DEBUG > TRACE > ALL -->
<!--Configuration后面的status，这个用于设置log4j2自身内部的信息输出，可以不设置，当设置成trace时，你会看到log4j2内部各种详细输出-->
<!--monitorInterval：Log4j能够自动检测修改配置 文件和重新配置本身，设置间隔秒数-->
<configuration status="INFO">
      <properties>
            <!-- 配置日志存放的文件夹 -->
            <property name="LOG_HOME">logs</property>
            <!-- 配置日志名称 -->
            <property name="LOG_NAME_HOME">my_sendemail-error</property>
        </properties>
     <!--先定义所有的appender-->
     <appenders>
         <!--这个输出控制台的配置-->
         <console name="Console" target="SYSTEM_OUT">
             <!--控制台只输出level及以上级别的信息（onMatch），其他的直接拒绝（onMismatch）-->
             <ThresholdFilter level="info" onMatch="ACCEPT" onMismatch="DENY"/>
             <!--输出日志的格式-->
             <PatternLayout pattern="[%d{yyyy/MM/dd HH:mm:ss-SSS}] [%p] ===> 【%F -- %L】：%m%n"/>
         </console>
         <!--文件会打印出所有信息，这个log每次运行程序会自动清空，由append属性决定，这个也挺有用的，适合临时测试用-->
         <RollingFile name="FileLog" fileName="${LOG_HOME}/${LOG_NAME_HOME}.log" filePattern="${LOG_HOME}/${LOG_NAME_HOME}-%d{yyyy-MM-dd}-%i.log" append="false">
             <!-- 只记录错误日志 -->
             <ThresholdFilter level="error" onMatch="ACCEPT" onMismatch="DENY"/>
             <!-- 记录的格式 -->
             <PatternLayout pattern="[%d{yyyy/MM/dd HH:mm:ss-SSS}] [%p] ===> 【%F -- %L】：%m%n - %msg%xEx%n"/>
             <!-- filepattern 中的日期格式精确位数决定了生成日志的日期单位， -->
             <Policies>
                  <!-- interval 决定该单位下的间隔数，如果在上面例子中 interval 设置为 1，那么就是1天生成一个日志； -->
                 <TimeBasedTriggeringPolicy interval="1" modulate="true" />
                  <!-- 文件最大为20M，超过则再生成一个文件 -->
                 <SizeBasedTriggeringPolicy size="20 MB" />
             </Policies>
             <!-- 每天的午夜触发 -->
             <CronTriggeringPolicy schedule="0 0 0 * * ?"/>
              <!-- 配置说明: 只处理位于logs文件夹下的文件,只处理*${LOG_NAME_HOME}*.log的文件,只处理最后一次修改时间超过5天的文件-->
             <DefaultRolloverStrategy>
                <Delete basePath="${LOG_HOME}/" maxDepth="1">
                    <IfFileName glob="*${LOG_NAME_HOME}*.log" />
                    <IfLastModified age="5d" />
                </Delete>
             </DefaultRolloverStrategy> 
         </RollingFile>
     </appenders>
     <Loggers>
        <!--过滤掉spring和mybatis的一些无用的DEBUG信息-->
        <logger name="org.springframework" level="TRACE" additivity="false"/>
        <!-- 打印SQL语句 -->
        <logger name="com.zxy.dpiserver.dao" level="TRACE" />
        <!-- Root level 是设置全局的默认信息显示级别，这里设置为info表示：info及以上的信息将被输出
        信息的级别大小为：fatal > error > warn > info > debug-->
        <Root level="debug">
          <AppenderRef ref="Console"/>  <!-- 仅有上述的Appenders配置还不够，这里还不能少，少了就不会在控制台输出 -->
          <AppenderRef ref="FileLog"/>  <!-- 仅有上述的Appenders配置还不够，这里还不能少，少了就不会写入文件，但会创建文件 -->
        </Root>
     </Loggers>
     
 </configuration>